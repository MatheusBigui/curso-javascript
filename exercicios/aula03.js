"use strict"

let nome = "Matheus"// variável global(porque está no primeiro nível(está colada)) que só pode ser acessada ou alterada no mesmo nível ou nos próximos, mas não nos anteriores
nome = "Anderson"

var idade = 16// variável global(porque está no primeiro nível(está colada)) que só pode ser acessada em qualquer nível
idade = 42

const genero = "masc"// variável global(porque está no primeiro nível(está colada)) que não pode ser alterada, é uma constante